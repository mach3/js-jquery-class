Class.create for jQuery

Function to create custom class just like prototype.js's "Class.create"

version: 1.2
author: mach3
requires: jQuery

example:
	
	[ Prototype.js Style ]
	
	var MyClass = Class.create();
	MyClass.prototype = {
		MY_EVENT:"myevent",
		config: null,
		initialize:function( config ){
			// Constructor
			$.extend( this.config, config );
		}
	};

	var myobj = new MyClass({hoo:"bar"});
	myobj.bind( myobj.MY_EVENT, function(){ alert("MY_EVENT") } );
	myobj.trigger( myobj.MY_EVENT );
	
	// inheritance
	var MySubClass = Class.create( MyClass );
	MySubClass.prototype = {
		initialize:function(){
		}
	};
	
	
	[ Simple Style ]
	
	var MyClass = Class.get({
		MY_EVENT:"myevent",
		config:null,
		initialize:function( config ){
			$.extend( this.config, config );
		}
	});
	
	// inheritance
	var MySubClass = Class.get({
		initialize:function(){
		}
	}, MyClass);


note:
	
	Created custom class automatically execute "initialize" method if it's defined,
	when the class is defined as a instance.

	The class can be used width jQuery's bind() and trigger() method.

	Thas's all. Simple one.

update:
	
	1.1 New release ( changed entirely )
	1.0 first release.
